import 'package:flutter/material.dart';

class UserNewFollowing extends StatelessWidget {
  const UserNewFollowing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}